<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="GliderSchool.Content.NavigationView"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:cal="clr-namespace:Caliburn.Micro.Xamarin.Forms;assembly=Caliburn.Micro.Platform.Xamarin.Forms"
             Title="Glider School">
    <ContentPage.Content>
        <StackLayout VerticalOptions="FillAndExpand">
            <ListView ItemsSource="{Binding Navigation}"
                      SelectedItem="{Binding SelectedNavigationItem}"
                      SeparatorVisibility="None"
                      VerticalOptions="FillAndExpand"
                      cal:Message.Attach="[Event ItemTapped] = [Action SelectNavItem]">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Title}" cal:View.Model="{Binding}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>